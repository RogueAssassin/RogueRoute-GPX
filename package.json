{
  "name": "gpx-route-generator",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": "24.18.0",
    "pnpm": "11.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter @rogue/gpx-web dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @rogue/gpx-web start",
    "console": "pnpm --filter @rogue/gpx-console start",
    "typecheck": "pnpm --filter @rogue/gpx-core build && pnpm -r typecheck",
    "test": "pnpm -r --if-present test && bash tests/download-osm.test.sh && bash tests/osrm-graph.test.sh"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^6.0.3"
  }
}
